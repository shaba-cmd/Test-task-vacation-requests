{
  "name": "vacation-requests-server",
  "version": "1.0.0",
  "private": true,  
  "description": "REST API для заявок на отпуск",
  "license": "ISC",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
